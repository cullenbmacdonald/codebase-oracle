{
  "SessionStart": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "bash -c 'if [ -f docs/history/index.yaml ]; then cat docs/history/index.yaml; else echo \"No history index found. Run /oracle:mine to generate.\"; fi'",
          "timeout": 5
        }
      ]
    }
  ]
}

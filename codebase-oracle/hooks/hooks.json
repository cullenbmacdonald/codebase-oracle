{
  "SessionStart": [
    {
      "matcher": ".*",
      "hooks": [
        {
          "type": "command",
          "command": "bash -c 'if [ -f docs/oracle/index.yaml ]; then cat docs/oracle/index.yaml; else echo \"No history index found. Run /oracle:mine to generate.\"; fi'",
          "timeout": 5
        }
      ]
    }
  ]
}
